# Frontend

Run (from `zprp/`):

```bash
make frontend_setup
make frontend_run
```

E2E tests (from `zprp/`):

```bash
make frontend_test
```

Backend URL (optional):

```bash
BACKEND_BASE_URL=http://127.0.0.1:8000 make frontend_run
```


