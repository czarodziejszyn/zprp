# Running model from docker

This module is used for running model from backend docker to calculate predicted price.

## `run_model.py`

This python program has 2 functions. First function returns model predicted price for $m^2$ for given coordinates and average price based on offers from 500m radius. Second function returns path to chart that shows feature importance for model used for prediction.
